O teste esta inacabado porém:
 -foi feito o consumo da API e seus dados podem ser visto no console
 -foi feito o filtro e o ordernar por

eu tive problemas com o servidor e com a node-modules por isso só foi enviado o js, o html e o css
foi utilizado nesse projeto html, css, js e bootstrap

O teste consistia em:
-Consumir a API;
-Mostrar os dados:
-Fazer o filtro;
-Fazer a ordenação;

mesmo com o teste inacabado espero ser considerada para a posição pois tenho muito a mostrar

